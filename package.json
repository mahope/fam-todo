{
  "name": "nestlist-monorepo",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "install": "cd apps/web && npm ci",
    "build": "cd apps/web && npx prisma generate && cd apps/web && npm run build",
    "start": "cd apps/web && npm start"
  }
}