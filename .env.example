
# Core
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
PUBLIC_BASE_URL=http://localhost:3000

# JWT (shared across web -> PostgREST/Realtime)
JWT_SECRET=dev_jwt_secret_change_me
BETTERAUTH_SECRET=dev_bauth_secret_change_me

# Optional keys for storage (only used by storage service)
ANON_KEY=dev_anon_key
SERVICE_ROLE_KEY=dev_service_key

# Realtime encryption (dev only)
REALTIME_ENC_KEY=dev_local_key
